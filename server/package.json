{
  "name": "ventix-meta-leads-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "dev": "node --experimental-sqlite --watch src/index.js",
    "start": "node --experimental-sqlite src/index.js",
    "test": "node --experimental-sqlite --test test/*.test.js",
    "hunter:check": "node --experimental-sqlite scripts/hunter-check.js",
    "owner:check": "node --experimental-sqlite scripts/owner-check.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.1.0",
    "playwright": "^1.49.0"
  }
}
