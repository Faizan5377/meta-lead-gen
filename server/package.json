{
  "name": "ventix-meta-leads-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "dev": "node --experimental-sqlite --watch src/index.js",
    "start": "node --experimental-sqlite src/index.js",
    "test": "node --experimental-sqlite --test test/*.test.js",
    "relevance:check": "node --experimental-sqlite scripts/relevance-check.js",
    "probe:feed": "node --experimental-sqlite scripts/probe-feed.js",
    "supabase:setup": "node --experimental-sqlite scripts/supabase-setup.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.1.0",
    "playwright": "^1.49.0",
    "@supabase/supabase-js": "^2.45.0",
    "pg": "^8.13.0"
  }
}
